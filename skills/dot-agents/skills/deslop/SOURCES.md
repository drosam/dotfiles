# Deslop Sources

Retrieved: 2026-09-24. Class: workflow-process; shape: reference-backed review with explicit selections.

| Source | Trust tier | Confidence | Usage constraints |
| --- | --- | --- | --- |
| Local skill/catalogs and history `f1ff2f87ff72e170bf897326c1a6f60b64cc308e` | canonical local | high | Preserve narrow scope, neighbor checks, numbered user picks |
| https://grugbrain.dev/ | explicitly attributed philosophy | high philosophy; unknown skill ancestry | Re-read live; no historical snapshot; no permission-bypass advice adopted |

Decision: replace automatic commit/reset with approved edits/checks. Require semantic equivalence and correct catalog traps: nil vs false, nested splats, dict errors, validation semantics, async contracts and external env values. Keep complexity/fence philosophy rather than deleting safeguards by pattern alone.

Coverage/profile: workflow preconditions, read-only scan, user gate, focused checks, failure handoff covered. Desk cases: safe-nav boolean coercion → keep; unknown fence → investigate; "looks good" → clarify; approved edits → no automatic commit.

Stop: attributed philosophy remains aligned; correctness fixes derive from local examples, not a claimed new upstream release. Gap: original skill implementation source unknown; retrieve owner URL if needed. Strict validator blocked by missing PyYAML; no language-runtime benchmark.
