#!/bin/bash

cd ~/Developer/dotfiles

rm ~/.zshrc

folders=(
  "alacritty" 
  "ghostty"
  "delta" 
  "gitconfig" 
  "nvim" 
  "skhd" 
  "tmux.conf"
  "tmux"
  "tmuxinator" 
  "zshrc" 
  "bin"
  "starship"
)

stow --dotfiles -t $HOME "${folders[@]}"

cd -
